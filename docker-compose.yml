services:
  postgres:
    image: postgres:16-alpine
    container_name: networking-postgres
    environment:
      POSTGRES_USER: networking
      POSTGRES_PASSWORD: networking
      POSTGRES_DB: networking_events
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
